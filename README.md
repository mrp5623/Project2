1. Open the directory you want to contain your project in terminal
2. Clone the repo into the directory with "git clone: https://github.com/mrp5623/Project2.git"
3. Build the project with "./build"
4. Change your directory to the correct folder with "cd build/Release"
5. Run the code with "./main.exe"
